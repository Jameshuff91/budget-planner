# Budget Planner Environment Variables

## OpenAI API Configuration (Optional)
# If set, this will be used as the default API key for Smart Categorization
# You can still override this in the app settings
NEXT_PUBLIC_OPENAI_API_KEY=sk-your-api-key-here

## Plaid Configuration (Optional)
# Only needed if you want to connect bank accounts
NEXT_PUBLIC_PLAID_CLIENT_ID=your-plaid-client-id
PLAID_SECRET=your-plaid-secret
NEXT_PUBLIC_PLAID_ENV=sandbox
PLAID_REDIRECT_URI=http://localhost:3000/plaid-redirect

## Plaid Webhook URL (Optional)
# Only needed for real-time transaction updates
# PLAID_WEBHOOK_URL=https://your-ngrok-url.ngrok-free.app/api/plaid/webhook

## Backend API URL (Optional)
# Only change if running backend on different port
# NEXT_PUBLIC_API_URL=http://localhost:3001/api

# ===== Claude Code API Keys (Optional) =====
# These are for Claude Code CLI tool, not the Budget Planner app
ANTHROPIC_API_KEY="your_anthropic_api_key_here"       # Required: Format: sk-ant-api03-...
PERPLEXITY_API_KEY="your_perplexity_api_key_here"     # Optional: Format: pplx-...
OPENAI_API_KEY="your_openai_api_key_here"             # Optional, for OpenAI/OpenRouter models. Format: sk-proj-...
GOOGLE_API_KEY="your_google_api_key_here"             # Optional, for Google Gemini models.
MISTRAL_API_KEY="your_mistral_key_here"               # Optional, for Mistral AI models.
XAI_API_KEY="YOUR_XAI_KEY_HERE"                       # Optional, for xAI AI models.
AZURE_OPENAI_API_KEY="your_azure_key_here"            # Optional, for Azure OpenAI models (requires endpoint in .taskmaster/config.json).
OLLAMA_API_KEY="your_ollama_api_key_here"             # Optional: For remote Ollama servers that require authentication.